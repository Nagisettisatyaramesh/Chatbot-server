node_modules/
dist/
build/
*.tsbuildinfo
.env
.env.local
server/prisma/dev.db
server/prisma/dev.db-journal
server/uploads/
# Runtime "database" -- per-website JSON stores, admin credentials, and
# uploaded files. Generated automatically on first use; never committed.
server/data/
*.log
.DS_Store
# Local scratch -- a throwaway repo clone used to verify a push, and a
# PPTX-generation working directory (its own package.json/node_modules).
verify-clone/
pptx-build/
